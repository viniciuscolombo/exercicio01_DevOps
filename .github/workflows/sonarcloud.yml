name: Deploy to Kind Cluster

on:
  workflow_call:
  push:
    branches: ["main"]
  workflow_dispatch:

jobs:
  build-and-analysis:
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: SonarCloud Scan
        uses: SonarSource/sonarcloud-github-action@v2
        with:
          args: >
            -Dsonar.projectKey=viniciuscolombo_exercicio01_DevOps
            -Dsonar.organization=viniciuscolombo
            -Dsonar.host.url=https://sonarcloud.io
            -Dsonar.sources=frontend/src
        env:
          SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}

      # Etapa comentada para evitar erro de "file not found" até que o arquivo ./scripts/sonar_check.sh seja criado
      # - name: Falhar se houver mais de 2 vulnerabilidades médias
      #   run: |
      #     sh ./scripts/sonar_check.sh
      #   env:
      #     SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
      #     SONAR_HOST_URL: https://sonarcloud.io
      #     SONAR_PROJECT_KEY: viniciuscolombo_exercicio01_DevOps

#      - name: Log in to Docker Hub
#        uses: docker/login-action@v3
#        with:
#          username: ${{ secrets.DOCKER_USERNAME }}
#          password: ${{ secrets.DOCKER_PASSWORD }}

#      - name: Build Docker image
#        run: docker build -t viniciuscolombo/satc-devops:0.0.2 .

#      - name: Push Docker image
#        run: docker push docker.io/viniciuscolombo/satc-devops:0.0.2

#      - name: Pull image
#        run: docker pull docker.io/viniciuscolombo/satc-devops:0.0.2

#      - name: Load image into Kind
#        run: kind load docker-image docker.io/viniciuscolombo/satc-devops:0.0.2 --name cluster-satc

#      - name: Deploy to cluster
#        run: kubectl apply -f deployment.yaml

      # - name: Run Trivy vulnerability scanner
      #   uses: aquasecurity/trivy-action@master
      #   with:
      #     scan-type: 'image'
      #     image-ref: 'docker.io/viniciuscolombo/satc-devops:0.0.2'
      #     format: json
      #     output: trivy-results.json
      #     severity: MEDIUM,HIGH,CRITICAL

      # - name: Upload Trivy results as artifact
      #   uses: actions/upload-artifact@v4
      #   with:
      #     name: trivy-results.json
      #     path: trivy-results.json

      # - name: Analyze Trivy results
      #   run: |
      #     if [ -f trivy-results.json ]; then
      #       VULN_COUNT=$(jq '[.Results[].Vulnerabilities[] | select(.Severity == "MEDIUM")] | length' trivy-results.json)
      #       echo "Quantidade de vulnerabilidades médias: $VULN_COUNT"

      #       if [ "$VULN_COUNT" -gt 2 ]; then
      #         echo "Pipeline bloqueada: mais de 2 vulnerabilidades médias encontradas."
      #         exit 1
      #       fi
      #     else
      #       echo "Arquivo de resultados do Trivy não encontrado."
      #       exit 1
      #     fi